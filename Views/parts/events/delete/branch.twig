<div class="item">
    <i class="large red code branch middle aligned icon"></i>
    <div class="content">
        {{ render('parts.events.meta', { event: event })|raw }}
        <div class="header">{{ event.payload.ref }} deleted in {{ render('parts.events.repo', {event: event})|raw }}</div>
        {{ render('parts.events.time', { event: event })|raw }}
    </div>
</div>
