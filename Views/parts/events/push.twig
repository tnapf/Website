<div class="item">
    <i class="large blue cloud upload alternate middle aligned icon"></i>
    <div class="content">
        {{ render('parts.events.meta', { event: event })|raw }}
        <div class="header">{{ event.payload.size }} commits pushed to {{ render('parts.events.repo', {event: event})|raw }}</div>
        {{ render('parts.events.time', { event: event })|raw }}
    </div>
</div>
